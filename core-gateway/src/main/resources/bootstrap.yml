spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        enabled: true
        service: collectplatform-core-gateway
      config:
        server-addr: 127.0.0.1:8848
        group-id: collectplatform
        name: core-gateway  # 对应 data-id